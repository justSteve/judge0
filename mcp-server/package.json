{
  "name": "judge0-mcp-server",
  "version": "1.1.0",
  "description": "MCP server for Judge0 code execution",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "judge0-mcp": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
